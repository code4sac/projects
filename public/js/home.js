import initializeNavigationBar from './navigationBar.js'

const home = () => {
  /**
   * Home scripts.
   * Usage:
   * ```js
   * home()
   * ```
   */
  initializeNavigationBar()
}

export default home